
 /* if else if: if is executed when condition is true elseif is checked multiple true condition elsif is false else is executed
  let x = 10;
  let y = 15;
  syntax;
  if (condition) {
      statements;
    }
   elseif(condition) {
    statements;
  }
  else 
  {
  statements;
  }

  */
  let x = 30;
  let y = 30;
  if (x > y)
  {
    console.log("x is greater than y");
  }
  else if(b>a)
  {
    console.log("y is greater than x");
  }
 else {
    console.log("x and y both are equal numbers");
  }
